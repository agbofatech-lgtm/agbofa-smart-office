package com.agbofa.smartoffice.app

import android.app.Application

/**
 * Phase 1 application entry only.
 *
 * No product engines are started here.
 * No workers, no database, no sync, no intelligence.
 */
class SmartOfficeApplication : Application()
